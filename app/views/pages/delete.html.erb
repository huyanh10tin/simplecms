<%= link_to("<< Back to List",pages_path,:class =>'back-link') %>
<div class="pages index">
	<h2>Delete Pages</h2>
	<%= form_for(@page,:method => 'delete') do |f| %>
	<p>Are you sure ?</p>
	<p class="reference-name"><%= @page.name %></p>			

	<div class="form-buttons">
			<%= f.submit("Delete Submited !") %>
	</div>
	<% end %>
</div>